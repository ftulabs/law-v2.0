{
  "name": "veritrade-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "The UI every VeriTrade platform runs. One codebase; the shells differ only in how they reach the API.",
  "scripts": {
    "codegen": "node scripts/codegen.mjs",
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
