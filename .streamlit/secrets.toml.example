# Copy to .streamlit/secrets.toml (gitignored) for local Streamlit, OR paste these
# keys into your deployment platform's Secrets UI (Streamlit Community Cloud, HF Spaces).
# The dashboard reads OPENROUTER_API_KEY from st.secrets when present.
OPENROUTER_API_KEY = ""
OPENROUTER_MODEL = "meta-llama/llama-3.3-70b-instruct:free"
