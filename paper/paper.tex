% VeriTrade, in ICLR 2027 format.
%
% Every number inside a strict region comes from \lnum{} or \claim{}, both
% defined in generated/, both derived from bench/out/metrics.json, which is
% derived from bench/out/runs/. `ledger paper` refuses to compile if any of them
% stops tracing back to a record.
%
% System-level figures (document counts, wall-clock, CER) are single-run
% measurements against live portals. They are reported as such, outside strict
% regions, and Section 5.1 says plainly that they are readings rather than rates.
%
% Anonymity is the style file's job while \iclrfinalcopy stays commented out.
\documentclass{article}
\usepackage{iclr2027_conference,times}
\usepackage{amsmath}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{url}
\usepackage{pgfplots}
\pgfplotsset{compat=1.16}
\usetikzlibrary{arrows.meta, positioning, fit, backgrounds, calc}

\input{generated/values.tex}
\input{generated/claims.tex}

\title{VeriTrade: A Verifiable End-to-End Harness for\\Digital Trade Regulation}

\author{Anonymous authors \\ Paper under double-blind review}

%\iclrfinalcopy

\begin{document}
\maketitle

\begin{abstract}
Regulatory indices are compiled by hand: an analyst is given a jurisdiction and a
legal question, searches official portals, reads statutes, and records which
article answers it. Automating this is usually framed as retrieval over a corpus
that has already been assembled. Three problems make that framing insufficient.
Generated citations hallucinate, and in law a fabricated authority is worse than
no answer. Even a correct answer is hard to verify, because verification means
returning to a source document the system never kept. And the law moves: an
instrument is amended, repealed, or cut across by a neighbouring one, so an
answer that was right is silently wrong later.

We present \textbf{VeriTrade}, an end-to-end harness specialised for
\textbf{digital trade regulation} and built on hybrid retrieval. It is given a
jurisdiction and a regulatory topic and \textbf{\emph{no source URLs}}, and it
discovers statutes on official portals, acquires them against bot defences and
encrypted catalogues, recovers text from scanned images, segments instruments
into articles, retrieves candidates with a script-aware hybrid ranker, maps each
to a codified legal test with a model that may never author a citation, and
emits a confidence with explicit routing to human review. It runs across six
Asia-Pacific economies in three writing systems, and every instrument it stores
carries a version identity so that a re-check against the portal is a query
rather than a re-crawl.

We contribute the system, its verifiability guarantee, an open implementation
that runs on small models, and a reproducible benchmark for verifiable
retrieval. On that benchmark, one tokenisation decision removes two entire
economies. \claim{C1} Both fixes are necessary and neither is sufficient, and in
three of the four configurations nothing raises, logs, or exits non-zero.
\claim{C6} Every number in this paper is re-derived from run records at compile
time, and the document does not build if one stops resolving.
\end{abstract}

\section{Introduction}

The Regional Digital Trade Integration Index scores an economy's digital-trade
regulation across twelve thematic pillars resolving to sixty-one legal
questions \citep{escap2024rdtii}. Each question is answered not with a
judgement but with an instrument, an article within it, and that article's text
quoted verbatim. Coding one economy today is a person reading statutes.

Automating this looks like retrieval, and the field has excellent benchmarks for
it. COLIEE has run statute and case-law retrieval for over a decade
\citep{goebel2023coliee}; LexGLUE assembles seven legal understanding tasks
\citep{chalkidis2022lexglue}; LegalBench extends to legal reasoning
\citep{guha2023legalbench}; BSARD poses exactly our question shape --- natural
language in, statutory article out \citep{louis2022bsard}. In every one of
them the corpus is handed to the system.

That is the right design for measuring a model, and it is why none of them can
see the failures this paper is about. Three define the task.

\textbf{Citations hallucinate.} An audit of commercial legal-research tools from
LexisNexis and Thomson Reuters, sold on their freedom from hallucination, found
fabricated or misattributed authority in between roughly one in six and one in
three responses \citep{magesh2024hallucination}, with higher rates on open-ended
case questions \citep{dahl2024fictions}. In law a fabricated authority is worse
than no answer, because it is actionable. \textbf{Answers are hard to verify},
because checking a legal claim means returning to a source document a system that
assembled its evidence transiently cannot offer; generative-search audits find
fluent answers routinely carrying unsupported citations
\citep{liu2023verifiability}. And \textbf{the law moves} --- instruments are
amended, repealed and cut across by neighbouring ones, so an answer that was
correct becomes silently wrong and nothing in a static corpus notices.

We treat the first as architectural rather than statistical: the model grades
evidence and never writes it. Law text, article numbers and URLs are carried from
extraction, and the one field the model may propose is discarded unless it occurs
literally in the retrieved snippet. The second and third are answered by storing
what was read --- every instrument carries a version identity and the portal
signal it came from, so re-checking an answer is a query rather than a re-crawl
(Section~\ref{sec:freshness}).

\paragraph{Contributions.}
\begin{enumerate}
\item \textbf{The first end-to-end harness for legal evidence retrieval.} Seven
stages from a bare jurisdiction-plus-topic input to cited, confidence-scored
indicator rows, with no seed URLs at any point (Section~\ref{sec:method}). The
domain specialisation is digital trade regulation, but nothing in the pipeline is
specific to it: a different body of law needs a codified test per indicator, a
reference set, and an entry point (Section~\ref{sec:portability}). To our
knowledge no prior published system combines autonomous source discovery with
extraction and indicator mapping in one evaluated pipeline; we state that as a
conjunction and engage the closest prior work in Section~\ref{sec:related}.

\item \textbf{Verifiability as an architectural property, not a metric.} The
model grades evidence and never writes it: law text, article numbers and URLs are
carried from extraction, and the one field the model may propose is discarded
unless it occurs literally in the retrieved snippet
(Section~\ref{sec:map}). Every row carries the verbatim source text, all four
confidence signals, and the portal version it was read from, so a reviewer can
reject a mapping directly rather than trusting a score
(Sections~\ref{sec:confidence},~\ref{sec:freshness}).

\item \textbf{An open implementation that runs on small models.} Recognition and
generation are both behind provider interfaces, and the pipeline is
engine-agnostic by construction. In a grader bake-off a 24B open-weights model
outscored three larger hosted alternatives, and a full run costs cents
(Section~\ref{sec:portability}).

\item \textbf{A reproducible benchmark for verifiable retrieval.} Offline,
deterministic, no network and no model: six economies, three scripts, two binary
treatments, five seeded draws. It measures \emph{reachability} --- whether a
query can see a cited instrument at all --- and the recall-versus-budget curve
above it (Section~\ref{sec:ablation}). Its numbers, tables and charts are
regenerated from records on every build.
\end{enumerate}

\section{Related work}
\label{sec:related}

\paragraph{Legal benchmarks assume the corpus.} COLIEE distributes a fixed
statute set \citep{goebel2023coliee}; LexGLUE and LegalBench evaluate
understanding and reasoning over supplied text
\citep{chalkidis2022lexglue,guha2023legalbench}; BSARD pairs questions with
22{,}600 pre-segmented Belgian articles \citep{louis2022bsard}. A benchmark whose
corpus is given cannot expose a discovery or acquisition failure, having already
paid the cost that dominates the task. A 2026 survey of legal LLM agents
\citep{liu2026legalagents} describes no system that locates sources without seed
URLs or a pre-built collection: its framing is that agents delegate authority to
legal databases and APIs, which assumes discovery away.

\paragraph{Acquisition and regulatory pipelines.} Pile of Law collects 256\,GB of
US legal material from an enumerated source list \citep{henderson2022pileoflaw}.
Closest to us is LOCUS \citep{peskoff2026locus}: browser automation against
anti-bot defences, OCR, segmentation into individual laws, per-segment
classification --- four of our seven stages, but over a \emph{known} set of
commercial vendors, in one country and one language, with no calibration or
review routing. REGREACT extracts typed compliance criteria
\citep{ali2026regreact}, ComplianceNLP monitors regulatory change end-to-end over
a curated graph of 12{,}847 provisions \citep{guo2026compliancenlp}, and a
multi-jurisdictional RAG system spans 68 jurisdictions and 242 documents
\citep{ford2026multijurisdictional}. All operate on document sets supplied as
input, which is the boundary this paper moves. On the multilingual side BLAD
assembles a statutory corpus for a low-resource jurisdiction
\citep{sakhawat2026blad} and \citet{moran2026multilingual} ask whether regulatory
auditing generalises beyond English; both take acquisition as given.

\paragraph{Retrieval and documents.} We combine BM25 \citep{robertson2009bm25}
with a dense bi-encoder \citep{karpukhin2020dpr} and a cross-encoder
\citep{nogueira2020monot5}, fused by convex combination rather than reciprocal
rank fusion \citep{cormack2009rrf}; \citet{bruch2022fusion} analyse that choice
and its normalisation. Mr.\ TyDi and MIRACL establish multilingual retrieval
\citep{zhang2021mrtydi,zhang2023miracl} but evaluate \emph{per language} on a
per-language index --- the interference in Section~\ref{sec:ablation} arises only
when scripts share one index. \citet{ogundepo2022whitespace} show character
$n$-grams substitute for language-specific tokenizers in BM25; we take that as
given and ask what a deployed system \emph{reports} when it has the wrong one.
OCR error degrades downstream NLP measurably \citep{vanstrien2020ocr};
\citet{aumiller2021segmentation} segment legal text by topical coherence where we
must recover formal article boundaries. For routing,
\citet{kamath2020selective} show softmax confidence fails under domain shift ---
load-bearing across six jurisdictions --- and our per-provision record is a
datasheet \citep{gebru2021datasheets} pushed down to the article.

\section{Problem formulation}
\label{sec:problem}

Given a jurisdiction $j$ and indicators $I=\{i_1,\dots,i_k\}$, each carrying a
codified legal test, the system must return tuples
$\langle\,\text{instrument},\,\text{article},\,\text{verbatim snippet},\,
\text{source URL},\,\text{confidence}\,\rangle$ such that the snippet occurs
character-for-character in the document at that URL and the article's operative
rule satisfies $i$'s test. Where nothing satisfies $i$ the system must say so and
name where it looked: a blank is indistinguishable from a failure to search.
Three constraints separate this from a retrieval benchmark --- \textbf{no seed
URLs} (the system is told which portals exist and nothing else; no document URL
or instrument name may be embedded), \textbf{scanned sources count}, and
\textbf{the answer may not be precomputed}, since evaluation is a live run.

The difficulty concentrates in four places, each of which returns a confident
empty answer when handled naively. Statutes sit behind portal software written
for human readers, so web search surfaces commentary more readily than the
statute. Portals resist automated collection. An instrument is not an answer:
returning a 618-article Act restates the question, so segmentation must be
reliable enough that the citation is checkable. And neighbouring indicators share
their vocabulary --- a prohibition on transferring data abroad and a permission
to transfer it subject to safeguards are written in almost the same words, and
retrieval correctly ranks both highly.

\section{Method}
\label{sec:method}

\begin{figure}[t]
\centering
\resizebox{0.94\linewidth}{!}{\input{fig_pipeline.tex}}
\caption{The seven stages, and the silent failure each can produce. Every one of
these was observed in development; each completes normally, exits zero, and
reports ``no provision found'' --- which is also the correct output for a
jurisdiction that genuinely has no such law.}
\label{fig:pipeline}
\end{figure}

Figure~\ref{fig:pipeline} gives the pipeline. We describe each stage and the
design decision that the multi-jurisdiction, multi-script setting forced.

\subsection{Discovery without seed URLs}
\label{sec:discover}

The system is configured with one domain per economy and no document URLs.
Queries are constructed from the indicators themselves, and split by sub-type:
short law-title fragments, and descriptive obligation phrases. The distinction
is operational --- Australia's register API matches against the Act title only,
so obligation phrases fired at it can never return a hit and merely spend the
query budget. Per-indicator terms are interleaved round-robin, so that
truncating the query list starves no indicator.

Portals then need portal-specific mechanisms, which we summarise here and detail
in Appendix~\ref{app:portals}. Malaysia serves its catalogue as an AES-GCM
payload whose key the page publishes for its own JavaScript; we decrypt exactly
as the page does. Singapore's browse listing ignores its pagination parameter
entirely, so the index is enumerated by unioning four sort windows. Australia
exposes an OData register. India publishes one record per \emph{section} through
a DSpace API, so provision boundaries are the publisher's own. Mongolia injects
its body client-side, so the document is reached through the export endpoint its
own toolbar calls.

\subsection{Acquisition}
\label{sec:acquire}

Fetching escalates from a browser-impersonating HTTP client to a stealth browser
only when a page proves to be an unrendered application shell. Bodies are
content-addressed by SHA-256, so two URLs serving identical bytes occupy one
file, and conditional requests avoid refetching.

Two decisions are worth stating. First, \texttt{robots.txt} is enforced with
unknown treated as denial: a server that cannot tell us its rules has not granted
permission. Second, where a portal states that its documents are not to be
downloaded --- China's national database returns an explicit
\texttt{download:\,0} permission block --- we stop rather than engineer around
it, and use a different official portal that publishes the same national laws as
ordinary pages. We record this because it is a design constraint with a coverage
cost, not a limitation we discovered late.

\subsection{Recognition and segmentation}
\label{sec:ocr}

Documents are triaged per page, not per file, because a file can carry a text
layer on some pages and images on others. Pages with a usable text layer are
read directly; the rest are rendered at 300\,dpi and passed to OCR. Recognition
language is selected per economy from a registry that records, for each engine,
whether a model for that script exists at all --- and an engine with no model is
not invoked. This matters because an engine can only emit characters in its
output dictionary: an audit of the shipped recognition dictionaries found the
Cyrillic model missing four Mongolian letters and sixteen of Kazakh's, and the
Latin model missing all forty-five precomposed Vietnamese tone forms. A missing
letter is not a low score; it is an impossibility that arrives as fluent text.

Segmentation recovers article boundaries per drafting tradition. Singapore and
Malaysia number sections; Australia typesets them bold with no keyword; China
uses an article marker that PDF extraction routinely splits with spaces; Mongolia numbers articles in laws but uses a
decimal clause style in ministerial circulars. Snippets are preserved verbatim:
page and heading sentinels are stripped from the snippet but retained in the
source text, so a provision spanning a page break still verifies exactly.

\subsection{Hybrid retrieval}
\label{sec:retrieval}

\begin{figure}[t]
\centering
\resizebox{0.96\linewidth}{!}{\input{fig_retrieval.tex}}
\caption{The retrieval stage. Retrieval is indicator-conditioned: the ranker runs
once per indicator over the economy's whole provision set.}
\label{fig:retrieval}
\end{figure}

Retrieval (Figure~\ref{fig:retrieval}) scores every provision against every
indicator. For provision $i$ and indicator $q$ the fused score is
\[
  s_i \;=\; \tfrac12\Big[\max\big(0,\;
      \alpha\,\tfrac{b_i}{\max_j b_j}
      + (1-\alpha)\,\tfrac{\cos_i + 1}{2}
      + \beta_i - \pi_i \big)\Big] \;+\; \tfrac12\,\sigma(c_i),
\]
with $\alpha=0.65$, $b$ the BM25 score max-normalised over the corpus, $\cos$ the
cosine of a multilingual bi-encoder mapped to $[0,1]$, $\sigma$ the logistic, and
$c$ the cross-encoder logit, defined as zero outside the rerank pool.

Three design choices carry the multi-script setting.

\paragraph{Tokenisation is script-aware, and the ASCII path is invariant.} A run
in a script without word spacing becomes overlapping character bigrams; spaced
scripts tokenise as words; accented Latin shares a character class with ASCII
rather than forming its own. The last point is not cosmetic: giving diacritics
their own class shreds Vietnamese into one-letter fragments that match any
document, returning plausible non-zero scores computed from noise. Critically,
pure-ASCII input tokenises bit-identically to the original English-only rule ---
pinned by regression test --- which is what licenses transferring parameters
swept on English corpora to the multilingual system.

\paragraph{Lexical and dense stages see different text.} The BM25 corpus includes
the law name for document-level discrimination through IDF; the embedded text
excludes it, because the law name otherwise dominates the embedding and makes
every section of a data-protection act look like a framework hit. Native-language
query terms are added to the lexical query only: the bi-encoder is cross-lingual
by construction, so an English question already reaches foreign text, while BM25
without native terms scores a flat zero.

\paragraph{The reranker is disabled rather than substituted.} The shipped
cross-encoder is English-only, and its score is fused at the same weight as
BM25 --- so an off-language reranker degrades the result rather than failing to
help. Selection is keyed on \emph{language}, not script: Vietnamese and
Indonesian are Latin-script and equally out of the English model's depth. Where
no model covers the language the stage is switched off entirely: two good
signals beat three when the third is arbitrary.

Shortlist size is
$k=\min(n,\,450,\,\max(40,\lceil 0.05n\rceil))$, except that a corpus of at most
80 provisions is graded in full, which makes retrieval a signal rather than a
gate --- our declared mitigation for economies whose retrieval parameters are
unmeasured.

\subsection{Indicator mapping}
\label{sec:map}

Each candidate is graded independently, and the prompt shows the target indicator
\emph{together with all of its siblings}, each with its own legal test. This
forces the explicit choice between adjacent indicators that retrieval cannot
make. The model returns structured fields --- operative rule, whether the target
is satisfied, a better sibling if the target is a mislabel, a graded legal-match
score, scope, and optionally a subsection --- and the contract is explicitly
symmetric between over-assignment and miss.

Two constraints define the anti-hallucination boundary. Non-English snippets are
reasoned over as written and never translated, because the verbatim column
\emph{is} the statute's text and a translation would make the citation false;
only the model's English rationale is generated. And the sole field the model may
contribute to the citation, a subsection reference, is kept only if it is
well-formed \emph{and} occurs literally in the snippet. Everything else in the
exported row is carried from extraction.

\subsection{Confidence and routing}
\label{sec:confidence}

Confidence is a weighted sum of four signals: the grader's legal-match judgement
(0.40), the fused retrieval score (0.25), snippet grounding (0.20), and scope
alignment (0.15). Grounding is a self-check --- whether the quoted snippet is
present in the source document --- and is the only signal computed without a
model. Two hard caps then apply: a scope-mismatched mapping on the
comprehensive-framework indicator is capped at 0.55, and a snippet sharing no
concept vocabulary with the pillar is capped at 0.45. Rows route to auto-accept,
human review, or quarantine. Indicators with no evidence receive an explicit
``no provision found'' row naming the portal searched; a blank is never emitted.

We state plainly that these six constants are hand-set. Fitting them needs
thousands of labelled provision-question pairs including hard negatives, and the
published answer key provides only final answers for three economies with no
near-miss negatives. Generating negatives ourselves would be grading our own
work. The design response is to surface all four signals and the verbatim
snippet on every row so a reviewer can reject a mapping directly, rather than to
present an uncalibrated number as a probability.

\subsection{Freshness: the law moves}
\label{sec:freshness}

An answer is only as current as the text behind it, so the unit of storage is a
\emph{law version} rather than a document. A law's identity is its economy and
landing URL; a version's identity is that law plus the portal's own version
signal --- a compilation date in Australia, a reprint date in Malaysia. Where a
portal exposes no such signal the content hash \emph{is} the version key, which
never lies about whether the text changed. Storing a new version supersedes the
previous one in the same transaction, so a retrieval pass sees each provision
once rather than once per historical edition.

This makes revalidation cheap enough to be routine. Checking whether a published
answer is stale means asking the portal for the version signal of the ten to
thirty instruments that answer actually cites, not re-crawling a jurisdiction.
Each is classified current, stale, or unverifiable --- and a portal that does not
respond is unverifiable rather than current, because silence is not confirmation.
The interference case is handled at grading time rather than storage time: the
model is shown the target indicator together with all of its siblings, so an
instrument that touches two adjacent obligations is scored against both rather
than being assigned to whichever was asked first (Section~\ref{sec:map}).

\subsection{Portability and model requirements}
\label{sec:portability}

Recognition and generation both sit behind provider interfaces, and the pipeline
selects among them per economy rather than globally: recognition language is
chosen from a registry that records, per engine, whether a model for that script
exists at all, and an engine with no model is not invoked. The consequence is
that adding an engine is a registry entry, and adding a jurisdiction is a
configuration entry naming its portal and adapter.

The generation stage is deliberately undemanding. It performs one bounded
judgement per candidate --- does this provision's operative rule satisfy this
legal test, or does a sibling fit better --- against a fixed schema, with no
long-context reasoning and no tool use. In a bake-off on 58 hand-read cases using
the shipped prompt, a 24B open-weights model reached F1 0.903 against 0.812,
0.800 and 0.786 for three larger hosted alternatives, the decisive difference
being recall (0.688 to 0.875) at the same price and roughly four times the speed.
We report the ranking cautiously: two of the four models reproduced exactly
across two runs and two moved by about 0.05, so only the first place is a firm
result. The operational claim is the weaker and more useful one --- the
architecture does not require a frontier model, and a complete run costs cents.

Porting to another body of law needs four things, none of them harness changes: a
codified test per indicator, a reference set from a source authoritative in that
domain, an entry point, and native query vocabulary per language --- which
Section~\ref{sec:ablation} shows is not optional in any multilingual
jurisdiction.

\subsection{The provenance harness}
\label{sec:provenance}

The pipeline's rule for law --- evidence is carried, never generated --- is
applied one level up to the paper. Experiments write one record per matrix point
carrying a content hash of its configuration and inputs; metrics collapse
records into cells, aggregating seed axes exactly once so no value reaches a
figure without its spread; and a checker walks this document, resolves every
cited value back to the records behind it, and fails the build on anything it
cannot ground. A condition matching two cells is an error rather than a
pick-the-first; a single-seed cell reports its spread as undefined rather than
zero; a failed run is recorded as failed rather than scored zero.

\section{Experiments}
\label{sec:experiments}

\subsection{System coverage}
\label{sec:coverage}

We ran the pipeline end to end across six economies.
\textbf{These are single runs against live portals, on the dates shown.} They are
readings, not rates: nothing has been run twice on different days, and portals
change. We report them because they establish that the pipeline completes across
three writing systems, and because the provisions-per-document column locates
where it does not yet work.

Appendix~\ref{app:coverage} gives the per-economy figures. The diagnostic column
is provisions per document: India's 423 documents yield 938 provisions because
most are one- or two-page circulars whose numbering the segmenter does not yet
recognise, which is a segmentation gap rather than a discovery one.

Of thirty-seven instruments the panel cites, twenty-seven are in our catalogue.
The other ten are regulator guidance, licence conditions and industry codes ---
not statutes, and not published where statutes are. That is a discovery gap, not
a retrieval one: no reranking reaches a document never enumerated.

\subsection{Retrieval parameters are measured, not chosen}
\label{sec:sweep}

Every parameter in Section~\ref{sec:retrieval} was swept against the expert
panel's own answer key on a stratified corpus of 383 laws and 36{,}590
provisions. The headline: raising the shortlist from 40 to 300 candidates moved
provision recall from 15/18 to 18/18, and measured end-to-end on production code
the parameter change moved recall from 11/18 to 17/18 --- Australia going from 1
of 5 cited provisions retrieved to 5 of 5, with the shortlist getting
\emph{cleaner} as it grew. Both fused signals are load-bearing: $\alpha=0.65$
gives 18/18 while pure BM25 or pure dense each give 16/18, and removing the
cross-encoder costs a target while using it alone costs three. Two tempting ideas
measured \emph{worse} and are recorded in Appendix~\ref{app:sweep} so they are
not re-proposed: a law-level prefilter is uniformly harmful at every budget, and
per-law slot reservation degenerates into breadth without depth.

\subsection{An economy can disappear without an error}
\label{sec:ablation}

The experiments above are single-configuration measurements against a live
system. This one is controlled, reproducible offline, and re-derived from records
at build time. We cross six economies with two binary treatments --- tokenisation
(monolingual vs.\ script-aware) and query vocabulary (English vs.\ plus native
statutory terms) --- over \lnum{seeds_per_cell} seeded draws of a distractor pool,
indexing all economies' instruments together in one pooled index of
\lnum{n_docs} documents. Instruments are rendered as their own portal serves
them; Mongolia's are recovered into Cyrillic by matching the numeric instrument
identifier inside the source URL, a language-independent join.

We report \emph{reachability}: the share of the panel's cited instruments that
receive any non-zero lexical score at all. A zero here is not a ranking that went
badly; it is a document the query cannot see.

\begin{table}[h]
\centering
\caption{Reachability by economy and condition. Latin-script economies are
unaffected by either treatment, by construction. The two non-Latin economies are
unreachable under three of four conditions, and no configuration raises or logs.}
\label{tab:reach}
\small
\begin{tabular}{llrrrr}
\toprule
& & \multicolumn{2}{c}{English terms} & \multicolumn{2}{c}{{}+ native terms} \\
\cmidrule(lr){3-4}\cmidrule(lr){5-6}
Economy & Script & Monolingual & Script-aware & Monolingual & Script-aware \\
\midrule
Singapore & Latin    & \lnum{reach_sg_ascii} & \lnum{reach_sg_script} & \lnum{reach_sg_ascii} & \lnum{reach_sg_script} \\
Australia & Latin    & \lnum{reach_au_ascii} & \lnum{reach_au_script} & \lnum{reach_au_ascii} & \lnum{reach_au_script} \\
Malaysia  & Latin    & \lnum{reach_my_ascii} & \lnum{reach_my_script} & \lnum{reach_my_ascii} & \lnum{reach_my_script} \\
India     & Latin    & \lnum{reach_in_ascii} & \lnum{reach_in_script} & \lnum{reach_in_ascii} & \lnum{reach_in_script} \\
China     & Han      & \lnum{reach_cn_ascii} & \lnum{reach_cn_script} & \lnum{reach_cn_ascii_nat} & \textbf{\lnum{reach_cn_full}} \\
Mongolia  & Cyrillic & \lnum{reach_mn_ascii} & \lnum{reach_mn_script} & \lnum{reach_mn_ascii} & \textbf{\lnum{reach_mn_full}} \\
\bottomrule
\end{tabular}
\end{table}

% ledger:strict
\claim{C1}
% ledger:endstrict

The structure of Table~\ref{tab:reach} is the finding. Script-aware tokenisation
is necessary and not sufficient: China stays at \lnum{reach_cn_script} once its
text is tokenisable, because an English query and a Chinese statute share no
tokens whatever the tokeniser does. Native vocabulary is likewise necessary and
not sufficient: under the monolingual tokeniser, adding Chinese phrases leaves
China at \lnum{reach_cn_ascii_nat}, because those phrases tokenise to nothing on
the way in.

The operational point is the mode, not the magnitude. Lexical scoring carries the
majority of the fused score, so an economy in this state contributes a uniform
zero to ranking, and the pipeline terminates normally reporting that no provision
was found --- which is a legitimate and common answer in a regulatory index.

\subsection{Widening the tokeniser is not free}

% ledger:strict
\claim{C2}
% ledger:endstrict

Under the monolingual rule the Cyrillic distractors tokenise to nothing: they
occupy the index but can never score. Making the tokeniser script-aware admits
them, which changes the document-frequency and length-normalisation statistics
BM25 shares across the collection \citep{robertson2009bm25}. Australia's
recall@10 falls from \lnum{r10_au_ascii} to \lnum{r10_au_script} although not one
byte of Australian tokenisation changed, while Malaysia rises from
\lnum{r10_my_ascii} to \lnum{r10_my_script}. Reachability, a per-document
property, is unchanged for every Latin economy; only rank-sensitive metrics move.
A team measuring only reachability would have concluded the change was free.

% ledger:strict
\claim{C3}
% ledger:endstrict

Reachability does not depend on which distractors were drawn, which is what one
should expect of a property of the query vocabulary and the tokeniser, and is
worth confirming rather than assuming. The rank-sensitive metrics do move with
the draw, which is why the seed is an axis of the grid and every cell carries its
spread.

\subsection{One shortlist budget does not fit six jurisdictions}
\label{sec:budget}

The shortlist size is a cost axis, not a free parameter: the grader is billed per
candidate, so a configuration that reaches every target by grading everything has
answered nothing useful. Figure~\ref{fig:curve} in Appendix~\ref{app:curve} plots recall against budget
under both treatments, with each economy's reachability drawn as a ceiling; the
curves have visibly different shapes.



% ledger:strict
\claim{C5}
% ledger:endstrict

A global top-$k$ tuned on one jurisdiction is therefore the wrong budget
everywhere else, and where the ceiling binds, buying more candidates cannot help:
the missing instruments are not ranked low, they are invisible.

% ledger:strict
\claim{C6}
% ledger:endstrict

Reachability is defined as an upper bound on recall and behaves as one at every
budget measured --- the internal-consistency check the benchmark exists to
support, and the reason we report it as the headline metric.

\subsection{Recognition quality}

On the one bundled scanned instrument with a human-checked transcript, RapidOCR
reaches 1.11\% character error at about 4\,s/page against a 5\% requirement. A
third engine initially measured \emph{0.00\%}, and that measurement was an
artifact: extracting nothing from an image-only PDF, it fell back to reading the
ground-truth transcript shipped beside the file and compared itself against the
answer key. Its true error is 95.56\% (Appendix~\ref{app:ocr}). A benchmark that
hands an engine its own reference is not a weak measurement but a measurement of
nothing --- and it reported a perfect score.

\section{Limitations}

\textbf{Confidence is uncalibrated}: four weights and two thresholds are
hand-set, ordering rows by reliability without being probabilities.
\textbf{There is no external baseline}: 18/18 target recall is good relative to
our own previous configuration and nothing else. The scorer we substituted caught
one of its own errors --- Mongolia initially scored 0 of 9 because the key names
instruments in English while the portal serves Mongolian. \textbf{Coverage is bounded by discovery, not ranking}:
ten of thirty-seven cited instruments are outside the catalogue because they are
not statutes, and closing that needs a separately enumerable index per instrument
type, most of which do not exist. \textbf{Most of the framework is unscored}: nine
of sixty-one indicators have codified tests; the other fifty-two run but have
never been checked for whether their definitions discriminate.
\textbf{System-level numbers are single runs} against live portals; only
Section~\ref{sec:ablation} is reproducible offline. \textbf{The controlled
experiment measures the lexical floor} --- instrument titles and reachability
rather than provision text and mapping F1 --- because provision-level corpora for
three economies do not exist and a live crawl is neither reproducible nor free.
We report the layer we can measure and decline to report the one we cannot.

\section{Conclusion}

A legal-evidence pipeline that cannot be audited is one whose failures are
invisible, and the multilingual ones are the most invisible of all: they present
as the output a correct system produces on a jurisdiction with no such law. We
built a harness that discovers its own sources, never lets a model author a
citation, stores what it read so an answer can be rechecked, and refuses to
publish a number it cannot trace to a run --- and pointing it at its own
retrieval layer found two economies silently absent.

\label{ledger:endmain}

% ICLR places both statements after the main text and before the references, and
% neither counts toward the page limit. \label{ledger:endmain} above is what the
% build measures the main text against, so they sit below it deliberately.

\section*{Reproducibility Statement}

The controlled experiment of Section~\ref{sec:ablation} runs offline with no
network, no credentials and no language model: both of its inputs --- the expert
panel's reference set and the harvested title catalogue --- are files in the
repository, so its records can be re-derived by anyone who clones it. The full
grid is \lnum{seeds_per_cell} seeds over the conditions of
Table~\ref{tab:reach}, and the code under test is imported from the shipped
system rather than reimplemented, because a benchmark that measures a copy of a
tokeniser measures the copy.

Every number in this paper that appears inside a verified region is a macro
resolved at compile time from those records, through the aggregation described in
Section~\ref{sec:provenance}; the document does not build if one stops resolving.
Generated tables and charts carry the hash of the metrics file they came from and
are re-checked against it at build time. Appendices~\ref{app:charts} and the
cell listing give the per-cell means, spreads and sample sizes behind every
figure.

The system-level runs of Section~\ref{sec:coverage} are \emph{not} reproducible
in this sense and we do not present them as though they were: they depend on
live government portals whose contents change, and each is a single measurement
on the date shown.

\section*{Ethics Statement}

This system retrieves public law from official government portals, and the manner
of retrieval raises two issues we treat as design constraints rather than
implementation details.

\textbf{Collection.} Crawling is rate-limited per host and honours
\texttt{robots.txt}, with an unreachable directive treated as denial rather than
permission --- a server that cannot state its rules has not granted any. Where a
portal declares that its documents are not to be downloaded we stop and use a
different official source that publishes the same instruments, accepting the
coverage cost (Section~\ref{sec:acquire}). One narrow, named exception relaxes
certificate validation for two hosts serving public statutes with expired
certificates; no credential is ever sent, and every fetched body is hashed so a
substituted document is detectable after the fact.

\textbf{Use.} The output feeds a regulatory index that informs policy, so a wrong
citation is not a benign error. This motivates the architecture rather than
sitting beside it: the model may not author a citation, every row carries the
verbatim source text and all four confidence signals, and rows below threshold
are routed to human review rather than published. We state explicitly in
Section~\ref{sec:confidence} that the confidence score is uncalibrated and
therefore orders rows by reliability without being a probability --- presenting
it as one would invite exactly the misplaced trust the system is built to avoid.

\bibliographystyle{iclr2027_conference}
\bibliography{references}

\appendix

\section{Portal mechanisms}
\label{app:portals}

Each portal required a different mechanism, and in every case the naive approach
returned a plausible empty result rather than an error. Dates are the days the
behaviour was verified against the live site.

\paragraph{Malaysia --- an encrypted catalogue.} The legislation portal answers
its catalogue endpoint with \texttt{\{"encrypted": true, "data": "\ldots"\}}. The
payload is AES-256-GCM laid out as a 12-byte IV, a 16-byte tag, then ciphertext,
and the key is a 64-hex-character constant the page publishes in its own source
so that its client-side JavaScript can decrypt it. We do exactly what that script
does. This is a transport wrapper rather than an access control --- the plaintext
is the same public catalogue of Malaysian legislation --- but a client that does
not implement it reads \emph{zero} Acts and raises nothing. Two catalogues are
enumerated, principal and amendment, because the principal one carries dated
reprints into which a later amendment has not yet been folded.

\paragraph{Singapore --- a pagination parameter that is ignored.} The browse
listing accepts a page parameter and disregards it: pages one, two and three
return byte-identical HTML at every page size, with or without the AJAX header
(verified 2026-08-01). The index is therefore not walkable. What the portal does
honour is sort order, so asking for the same list ascending and then descending
returns its first and last windows, and their union is the whole index whenever
the total is at most twice the page size. Four windows over two sort keys cover
the 524 current Acts. They do \emph{not} cover the 5{,}843 subsidiary
instruments, and the shortfall is logged as incomplete rather than passed over in
silence. The same portal throttles a burst by answering HTTP 202 with an empty
body rather than 429, so a naive client reads success and an empty page.

\paragraph{Australia --- multi-volume compilations.} The register exposes an
OData API whose filter matches against the Act \emph{title} only, so descriptive
obligation phrases can never hit and are suppressed for this portal. Large Acts
are published as multiple volumes, and for those the single-file PDF URL returns
404 --- the volume number must be appended. Before this was handled the 404 was
silent: the fetch failed, the pipeline fell back to the JavaScript landing page,
and the Act contributed one junk provision instead of roughly eight hundred. It
struck exactly the largest Acts, which is where retention and government-access
provisions live. Volumes must also be extracted and segmented \emph{separately}
before merging: concatenating them first makes the second volume's opening body
look like a table of contents, which deleted an entire data-retention Part.

\paragraph{India --- one record per section.} India Code exposes a DSpace API
that publishes each \emph{section} as its own item, so provision boundaries are
the publisher's rather than ours and no OCR or segmentation heuristic is
involved. Two constraints are non-obvious. The API silently caps a page at 100
items however many are requested, while reporting a page count computed as though
it had returned the larger number --- so a client that trusts it loses most of a
long Act with no error. And queries must be quoted phrases: the unquoted form of
a cross-border data query returns the Commercial Courts Act, a highways rule and
a police manual, with the actual data-protection statute absent.

\paragraph{Mongolia --- a body that is not in the page.} The detail page injects
its content client-side; the roughly twelve thousand Cyrillic characters in the
served shell are navigation, a modal and a sector index. The document is reached
through the export endpoint the page's own toolbar calls, which returns
Word-flavoured HTML --- so no OCR and no binary parse. Titles are matched
per-word rather than as phrases, because Mongolian word order differs from the
answer key's and the portal's own filenames contain missing spaces; per-word
matching also carries agglutination in the right direction, since a stem reaches
its declined forms. One detail decided whether the economy worked at all:
converting block-level elements to newlines \emph{before} stripping tags. Without
it the law arrives as 34{,}196 characters of correct Mongolian on a single line,
from which a line-anchored article pattern finds nothing --- and the run
completes, extracts, maps and reports no provision found.

\paragraph{China --- where we stop.} The national law database's own API returns
a permission block containing \texttt{download:\,0}, and the document path
resolves through a signed reader on an internal host. That is the operator
stating the documents are not to be downloaded, so we do not engineer around it.
The cyberspace administration's portal publishes the same national laws as
ordinary server-rendered pages, and that is the lane used. The cost is real: the
provincial tier remains reachable but is the wrong tier for national indicators.

\section{Recall against shortlist budget}
\label{app:curve}

\begin{figure}[t]
\centering
\input{fig_curve.tex}
\caption{Recall against shortlist budget, with reachability ceilings for the two
non-Latin economies. Every coordinate is resolved from run records at compile
time. The curves have different shapes: extra budget is worth a great deal in
some jurisdictions and nothing in others.}
\label{fig:curve}
\end{figure}

\section{Recognition benchmark}
\label{app:ocr}

On the one bundled scanned instrument with a human-checked transcript, RapidOCR
reaches 1.11\% character error at about 4\,s/page and PaddleOCR 0.00\% at
585\,s/page, against a 5\% requirement. We report a third number because it is
more instructive: MarkItDown initially measured 0.00\% error, and the measurement
was an artifact --- extracting nothing from an image-only PDF, it fell back to
reading the ground-truth transcript shipped beside the file and compared itself
against the answer key. Measured on a copy in a directory without that sidecar,
its true error is 95.56\%. The fix was to exclude sidecar-reading engines from
measurement and run the benchmark on an isolated copy. We record it because a
benchmark that hands an engine its own reference is not a weak measurement; it is
a measurement of nothing, and it reported a perfect score.


Measured on the bundled scanned notice: RapidOCR 1.11\% character error at
4.07\,s/page; PaddleOCR 0.00\% at 584.56\,s/page, a 143$\times$ penalty for an
engine of equivalent accuracy; MarkItDown 95.56\% once measured on an isolated
copy. The fix was to exclude sidecar-reading engines from measurement and to run
the benchmark in a directory containing no reference transcript.

\section{End-to-end coverage}
\label{app:coverage}

\begin{table}[h]
\centering
\caption{End-to-end single runs. Provisions per document is the diagnostic
column: India's 423 documents yield 938 provisions because most are one- or
two-page circulars whose numbering the segmenter does not yet recognise.}
\label{tab:coverage}
\small
\begin{tabular}{llrrrrr}
\toprule
Economy & Pillar & Documents & Provisions & Prov./doc. & Rows & Wall clock \\
\midrule
Singapore & 7   & 22  & 5{,}237 & 238 & 107 & 13 min \\
Malaysia  & 6+7 & 25  & 5{,}483 & 219 & 46  & 27 min \\
Australia & 6+7 & 18  & 2{,}196 & 122 & 29  & 11 min \\
China     & 7   & 18  & 559     & 31  & 51  & 8 min  \\
Mongolia  & 7   & 18  & 272     & 15  & 40  & 5 min  \\
India     & 7   & 423 & 938     & 2   & 44  & 7 min  \\
\bottomrule
\end{tabular}
\end{table}

\section{Retrieval parameter sweep}
\label{app:sweep}

Parameters were swept against the panel's own answer key on a stratified corpus
of 383 laws and 36{,}590 provisions, scoring whether the specific provision the
panel named reaches the shortlist. Raising the shortlist from 40 to 300
candidates moved provision recall from 15/18 to 18/18. Fusion ablations at the
chosen budget: $\alpha=0.65$ gives 18/18, $\alpha=0.50$ gives 17/18, and pure
BM25 or pure dense each give 16/18 --- so both signals are load-bearing.
Removing the cross-encoder costs a target; using it alone costs three.

Two negative results are worth recording because both are tempting. A law-level
prefilter is \emph{uniformly worse} at every budget: a law whose title is about
something else still holds the one provision that answers the indicator, and a
law-level gate discards it before any provision is considered. And reserving
shortlist slots per law degenerates on a multi-hundred-law corpus into
breadth-without-depth. Measured end-to-end on production code, the parameter
change moved provision recall from 11/18 to 17/18, with Australia going from 1
of 5 cited provisions retrieved to 5 of 5 --- and the shortlist got
\emph{cleaner} as it got larger.


\section{Worked segmentation and retrieval examples}
\label{app:cases}

Three instruments, one per script, walked through the same code the controlled
experiment measures. The aggregate result of Section~\ref{sec:ablation} says an
economy is unreachable; these show why, token by token. The Singaporean case is
the control --- Latin script throughout, so no condition changes anything.

Native titles are reproduced as images. This is not a stylistic choice: the TeX
installation used for this build has no CJK package and no Cyrillic encoding, and
transliteration is not available to us, because the pipeline's own rule is that
statutory text is carried and never rewritten. What appears below is the
statute's own text rather than a romanisation of it. Non-Latin tokens are counted
rather than printed for the same reason, and counted separately from Latin ones
so that a mixed-script query shows which half is doing the work.

\input{cases/cases.tex}

\section{Generated charts}
\label{app:charts}

Emitted by the figures stage from the same metrics file as every table above.
Bars are cell means; whiskers run min to max across the seed axes. A cell
measured at one seed draws no whisker, because a whisker of zero would read as a
measured spread of zero.

\subsection{Reachability}
\input{generated/figures/t_reach_reachable.tex}

\subsection{recall@5}
\input{generated/figures/t_reach_recall_at_5.tex}

\subsection{recall@10}
\input{generated/figures/t_reach_recall_at_10.tex}

\section{Full cell listing}

\subsection{Reachability}
{\scriptsize\input{generated/tables/t_reach_reachable.tex}}

\subsection{recall@5}
{\scriptsize\input{generated/tables/t_reach_recall_at_5.tex}}

\subsection{recall@10}
{\scriptsize\input{generated/tables/t_reach_recall_at_10.tex}}

\end{document}
