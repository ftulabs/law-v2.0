% RDTII-Bench, in ICLR 2027 format.
%
% Every number in this document comes from \lnum{} or \claim{}, both defined in
% generated/, both derived from bench/out/metrics.json, which is derived from
% bench/out/runs/. Nothing numeric here is typed by hand, and `ledger paper`
% refuses to compile if any of it stops tracing back to a record.
%
% Anonymity is the style file's job while \iclrfinalcopy stays commented out.
% `ledger paper` fails the build if anyone uncomments it before acceptance.
\documentclass{article}
\usepackage{iclr2027_conference,times}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{url}
% Charts are pgfplots rather than an included bitmap: `ledger figures` emits
% them as LaTeX, so they are deterministic, diffable, and verified against the
% metrics at build time like every other generated asset.
\usepackage{pgfplots}
\pgfplotsset{compat=1.16}
\usetikzlibrary{arrows.meta, positioning, fit, backgrounds}

\input{generated/values.tex}
\input{generated/claims.tex}

% The break is placed by hand: the title is set in small caps, which is wide
% enough that letting LaTeX choose hyphenates "Multilingual" across the line.
\title{RDTII-Bench: An Auditable Harness\\for Multilingual Legal Evidence Extraction}

\author{Anonymous authors \\ Paper under double-blind review}

%\iclrfinalcopy

\begin{document}
\maketitle

\begin{abstract}
Automated extraction of regulatory evidence from primary law is increasingly
deployed across jurisdictions, but it is almost never evaluated across the
scripts those jurisdictions actually publish in. We present a harness that treats
a legal-evidence pipeline the way an experiment is treated: every published
number is carried from a recorded run rather than typed, and every stage refuses
rather than guesses. We instantiate it on the RDTII 2.1 regulatory framework over
six Asia-Pacific economies publishing in three writing systems, and use it to
ablate a single design decision --- how text is tokenised for lexical retrieval
--- against the reference answers of the framework's own expert panel.
\claim{C1} Both factors are necessary and neither is sufficient, and in three of
the four configurations the failure is silent: the pipeline completes, raises
nothing, and reports an absence of law that is indistinguishable from a genuine
one. \claim{C2} \claim{C3} The harness, not the pipeline, is the contribution:
its methodology is domain-general, and we describe what porting it to another
body of law requires.
\end{abstract}

\section{Introduction}

Regulatory indices are built by hand. An analyst is given an economy and a topic,
searches official portals, reads statutes, and records which provision satisfies
which indicator. The ESCAP Regional Digital Trade Integration Index alone spans
eleven thematic pillars over forty-four sub-pillars, each requiring a coder to
locate and justify primary law in the jurisdiction's own language
\citep{escap2024rdtii}. The work is slow, expensive and difficult to reproduce,
and it is a natural target for automation: retrieve candidate provisions, ask a
language model whether each satisfies a codified legal test, and export the
accepted mappings with citations.

Systems of this shape are being built and sold. They are also, measurably, not
yet trustworthy. In the first pre-registered evaluation of commercial
AI legal-research tools, \citet{magesh2024hallucination} found that products
from LexisNexis and Thomson Reuters --- retrieval-augmented, and marketed on
their freedom from hallucination --- fabricated or misattributed authority
between 17\% and 33\% of the time. The response from the research community has
mostly been better benchmarks of legal \emph{reasoning}
\citep{chalkidis2022lexglue, guha2023legalbench} and better legal
\emph{retrieval} \citep{goebel2023coliee}. Those are necessary. They are also
evaluations of the model, and the failures we describe here are not in the model.

Three failures recur in systems of this kind, and they compound:

\begin{itemize}
\item \textbf{The model authors the evidence.} A generated citation, article
number or quotation cannot be checked against anything. Once a model is permitted
to write the text it is also citing, the output is unfalsifiable --- which is
precisely the regime in which the hallucination rates above were measured.
\item \textbf{The paper drifts from the run.} Numbers are copied into prose by
hand, the experiment is re-run, and the prose is not. Surveys of machine-learning
reproducibility find that no paper in a sample of four hundred documented
everything needed to reproduce it, and that provenance over the artifacts of a
pipeline --- data, code, environment, seeds --- is the missing discipline rather
than an optional extra \citep{samuel2020provenance}. The abstract and the table
disagree, and nobody notices because nothing checks.
\item \textbf{The evaluation is monolingual.} The system is developed against
English-language jurisdictions and shipped to others. Multilingual retrieval
resources exist \citep{zhang2021mrtydi, zhang2022miracl}, but a system evaluated
per language, on a corpus already in that language, never encounters the
condition that actually breaks: one index, several scripts, one tokenisation
rule. The failure that follows is not a crash; it is a quiet zero.
\end{itemize}

This paper is about a harness that closes all three, and about what the harness
found when it was pointed at its own pipeline.
\subsection{Contributions}

\begin{enumerate}
\item \textbf{An auditable harness for legal evidence extraction.} One invariant
--- every number in a published artifact is carried from a recorded run, never
typed by a human and never written by a model --- enforced by a checker that
walks the paper source and fails the build on any value it cannot ground. To our
knowledge this is the first application of run-level provenance enforcement to a
legal-evidence pipeline, and it is the same rule the pipeline already applies to
legal text, lifted one level up (Section~\ref{sec:harness}).
\item \textbf{A multi-region, multi-script evaluation.} Six economies, three
writing systems, nine indicators, graded against the reference answers recorded
by the framework's own expert panel (Section~\ref{sec:setup}).
\item \textbf{A measured account of a silent multilingual failure.} We show that
a monolingual tokenisation assumption removes an entire economy from retrieval
without raising anything, that fixing tokenisation alone does not recover it, and
that the fix is not free for the languages that already worked
(Section~\ref{sec:results}).
\item \textbf{A portable methodology.} The harness couples to a project through a
single manifest; we set out what a different body of law has to supply
(Section~\ref{sec:extending}).
\end{enumerate}

\section{The harness}
\label{sec:harness}

\subsection{The invariant}

The pipeline under study already runs on an anti-hallucination rule: legal text,
citations and URLs are \emph{carried from extraction, never generated}. The model
grades evidence; it never authors it. A cited snippet must be verifiably present
in the source document before the mapping is trusted, which makes every mapping
checkable against a document rather than against the model's confidence in
itself.

The harness applies exactly that rule one level up:

\begin{quote}
Every number, table and figure in a published artifact is carried from a recorded
run, never typed by a human and never written by a model.
\end{quote}

Same function, different domain. The pipeline grounds legal snippets against
source law; the harness grounds paper numbers against run records. No language
model runs anywhere in the path that produces a number, a table or a claim --- the
harness is a deterministic toolchain, and models run only \emph{inside} the
experiments it schedules. This is what makes the reproducibility statement in
this paper checkable rather than asserted.

\subsection{Stages}

The harness is a set of commands with file inputs and file outputs, orchestrated
by a makefile rather than a scheduler, so that any stage can be re-run alone and
a broken stage never blocks the others. A project declares itself through one
manifest; the harness never vendors project code, it imports the entry points the
manifest names.

\begin{itemize}
\item \texttt{spec} validates the manifest and resolves entry points.
\item \texttt{run} executes the experiment grid, one record per point. A
record's filename \emph{is} the hash of its task, its full configuration and its
input data, so resume is correct rather than merely convenient: a record produced
under a different configuration cannot land on the same name, and so cannot be
silently reused.
\item \texttt{metrics} collapses records into cells. The seed axes are aggregated
away here and nowhere else, so by the time a number is visible to a figure or a
claim it already carries its sample size, standard deviation, extremes and
spread. A single lucky seed cannot be published as a fact.
\item \texttt{figures} generates every asset the paper includes, deterministically.
\item \texttt{verify} walks the paper source, resolves every cited value back
through the metrics to the records behind it, and exits non-zero on anything it
cannot ground.
\item \texttt{paper} compiles the document, but runs \texttt{verify} first and
refuses to produce a PDF if any number in the source is unbacked.
\end{itemize}

\begin{figure}[t]
\centering
\begin{tikzpicture}[
  >=Stealth, font=\scriptsize,
  stage/.style={draw, rounded corners=2pt, fill=blue!8, minimum height=5.6mm,
                minimum width=11mm, font=\scriptsize\ttfamily, inner xsep=2pt},
  art/.style={font=\scriptsize\itshape, text=black!65, inner xsep=1pt},
  gate/.style={draw, rounded corners=2pt, fill=red!8, minimum height=5.6mm,
               minimum width=12mm, font=\scriptsize\ttfamily},
  flow/.style={->, semithick, black!70},
  check/.style={->, dashed, red!60!black, semithick},
]
% forward chain: artifacts and the stages between them
\node[art]                        (man)    {project.yaml};
\node[stage, right=3.2mm of man]  (spec)   {spec};
\node[stage, right=3.2mm of spec] (run)    {run};
\node[art,   right=3.2mm of run]  (recs)   {runs/};
\node[stage, right=3.2mm of recs] (met)    {metrics};
\node[art,   right=3.2mm of met]  (mj)     {metrics.json};
\node[stage, right=3.2mm of mj]   (fig)    {figures};
\node[art,   right=3.2mm of fig]  (assets) {assets};
\foreach \a/\b in {man/spec, spec/run, run/recs, recs/met, met/mj, mj/fig, fig/assets}
  \draw[flow] (\a) -- (\b);

% the publishing row, 18mm below, with verify between prose and the compiler
\node[art,   below=18mm of man]    (prose) {paper.tex};
\node[gate,  right=3.2mm of prose] (ver)   {verify};
\node[stage, right=3.2mm of ver]   (pap)   {paper};
\node[art,   right=3.2mm of pap]   (pdf)   {paper.pdf};
\draw[flow] (prose) -- (ver);
\draw[flow] (ver) -- (pap);
\draw[flow] (pap) -- (pdf);

% verify walking backward. Three checks, drawn as a fan so no label sits on a
% box: what each one re-derives is said in the caption instead.
\draw[check] (ver.north) -- (recs.south);
\draw[check] (ver.north) -- (mj.south);
\draw[check] (ver.north) -- (assets.south);
\node[font=\scriptsize\itshape, text=red!55!black, below=1.2mm of ver]
  {re-derives every cited value; a failure blocks the compile};
\end{tikzpicture}
\caption{The harness. Solid arrows are artifacts flowing forward. Dashed arrows
are \texttt{verify} walking backward from the prose to the records --- resolving
each cited value through \texttt{metrics.json} to the runs that produced it, and
checking that every generated asset still matches. Prose is the only hand-written
input, and it is the one input that is checked. No language model appears
anywhere on this diagram: models run \emph{inside} the experiments \texttt{run}
schedules, never in the path that produces a number.}
\label{fig:harness}
\end{figure}

\subsection{What refusal means}

Three decisions are worth defending, because each is a place where the obvious
implementation would have been to guess.

\textbf{An ambiguous selector raises.} A claim names a number by naming a
condition. A condition matching two cells is an error, not a pick-the-first: a
published number must not depend on dictionary ordering.

\textbf{One seed measures nothing about variability.} Where a cell has a single
run, the standard deviation is reported as undefined rather than as zero. Zero
would read as \emph{we measured stability and found it}.

\textbf{A failed run is recorded as a failure.} It is never scored zero and never
averaged in as though it were a measurement. A cell in which every run failed
keeps its place in the output with no metrics attached, and any claim that cites
it raises rather than quietly returning zero.

The same instinct governs the pipeline. An indicator with no supporting provision
emits an explicit \emph{no evidence found} row rather than a blank, and instruments
that are draft or repealed are dropped from the output rather than flagged in a
notes column --- a warning nobody reads is not a control.

\section{Experimental setup}
\label{sec:setup}

\subsection{Task}

RDTII 2.1 codifies nine indicators across two data-policy pillars --- cross-border
data movement, and domestic data protection. Each indicator carries a legal test:
the operative rule a provision must enact to satisfy it. The retrieval question
is prior to and strictly harder than the grading question, because it bounds it:
a provision that never reaches the shortlist cannot be graded, so it can never be
answered. We therefore measure the retrieval floor.

\subsection{Corpus and gold}

The evaluation corpus is built from two files, both checked into the repository,
so that a run needs no network, no credentials and no language model, and can be
re-derived by anyone who clones it.

The reference set is the expert panel's own record: for each economy and
indicator, the instrument the panel accepted as evidence. It covers six
economies --- Singapore, Australia, Malaysia, India, China and Mongolia --- and
yields \lnum{pairs_in} gold pairs for India, \lnum{pairs_cn} for China and
\lnum{pairs_mn} for Mongolia, among others.

\textbf{A document is the title as its own portal serves it.} This matters more
than it sounds. The panel worked in English and recorded Chinese instruments
bilingually, and Mongolian instruments under English names only. A crawler on a
Chinese portal sees only the Chinese title; a crawler on the Mongolian portal
sees only Mongolian. Indexing the panel's English glosses would measure the
panel's spreadsheet rather than the corpus a system actually meets, and would
conceal the failure this experiment exists to expose. We therefore keep the
native title wherever the record carries one. For Mongolia, where it does not, we
recover it by matching the numeric instrument identifier inside the source URL
against the portal's own catalogue --- a language-independent join, and the same
one the project adopted after discovering that scoring Mongolia at zero had been
a measurement error rather than a result. This recovers \lnum{nonlatin_mn} of
Mongolia's instruments into their own script; \lnum{nonlatin_cn} of China's are
native by construction. Unresolved instruments keep their English name and are
counted, so the extent to which the Mongolian arm is genuinely a Cyrillic task is
a reported number rather than an assumption.

Distractors are drawn from a harvested catalogue of Mongolian legal titles, which
is exactly what a distractor pool should be: real legal titles in a real script,
with no provision text, no indicator and no mapping. Each cell indexes the gold
instruments of all six economies together with a seeded sample of that pool,
giving \lnum{n_docs} documents. One pooled index rather than six is deliberate:
it is the setting the claim is about, since a harness serving several regions
holds them in one corpus, and the question is whether indexing Mongolia alongside
Singapore degrades either.

\subsection{Conditions}

The grid crosses economy with two binary treatments, repeated over
\lnum{seeds_per_cell} seeded draws of the distractor pool.

\textbf{Tokenisation} is either \emph{monolingual} --- the original rule, which
matches runs of ASCII letters and digits --- or \emph{script-aware}, which routes
spaced scripts to word tokens and no-space scripts to character bigrams. The
script-aware tokeniser is imported from the shipped system rather than
reimplemented: a benchmark that measures a copy of the component under test
measures the copy. The monolingual rule is reproduced as the control arm, and the
shipped implementation holds its ASCII path byte-identical to it so that
previously swept retrieval parameters continue to hold.

\textbf{Vocabulary} is either the indicators' English query terms alone, or those
terms plus native statutory phrases in the economy's own language. Native terms
are added, never substituted, so that a wrong guess can only fail to match and
can never displace a term that was working.

\subsection{Metrics}

\begin{itemize}
\item \textbf{Reachability} --- the share of an economy's gold pairs whose
instrument receives a non-zero lexical score. This is the silent-failure metric.
A zero here is not a ranking that went badly; it is a document the query cannot
see at all.
\item \textbf{recall@5} and \textbf{recall@10} --- the share whose instrument
reaches the top five, and the top ten, of the pooled index. Two budgets rather
than one, because the shortlist size is what the grader is billed for: five
candidates cost half of ten, and the difference between the two is the price of
the cheaper budget.
\item \textbf{MRR} --- mean reciprocal rank over the full ranking, zero when
unreachable. Ties are broken pessimistically: an instrument tied with fifty
others ranks behind all of them.
\end{itemize}

\section{Results}
\label{sec:results}

\subsection{An economy can vanish without an error}

\begin{table}[h]
\centering
\caption{Reachability by economy and condition. Latin-script economies are
unaffected by either treatment, by construction. The two non-Latin economies are
unreachable under three of four conditions --- and no configuration raises,
logs or fails.}
\label{tab:reach}
\begin{tabular}{llrrrr}
\toprule
& & \multicolumn{2}{c}{English terms} & \multicolumn{2}{c}{{}+ native terms} \\
\cmidrule(lr){3-4}\cmidrule(lr){5-6}
Economy & Script & Monolingual & Script-aware & Monolingual & Script-aware \\
\midrule
Singapore & Latin    & \lnum{reach_sg_ascii} & \lnum{reach_sg_script} & \lnum{reach_sg_ascii} & \lnum{reach_sg_script} \\
Australia & Latin    & \lnum{reach_au_ascii} & \lnum{reach_au_script} & \lnum{reach_au_ascii} & \lnum{reach_au_script} \\
Malaysia  & Latin    & \lnum{reach_my_ascii} & \lnum{reach_my_script} & \lnum{reach_my_ascii} & \lnum{reach_my_script} \\
India     & Latin    & \lnum{reach_in_ascii} & \lnum{reach_in_script} & \lnum{reach_in_ascii} & \lnum{reach_in_script} \\
China     & Han      & \lnum{reach_cn_ascii} & \lnum{reach_cn_script} & \lnum{reach_cn_ascii_nat} & \textbf{\lnum{reach_cn_full}} \\
Mongolia  & Cyrillic & \lnum{reach_mn_ascii} & \lnum{reach_mn_script} & \lnum{reach_mn_ascii} & \textbf{\lnum{reach_mn_full}} \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\begin{tikzpicture}
\begin{axis}[
  ybar, width=\linewidth, height=5.4cm,
  bar width=6.5pt, enlarge x limits=0.12,
  ymin=0, ymax=1.0, ylabel={reachability}, ylabel style={font=\small},
  symbolic x coords={Singapore, Australia, Malaysia, India, China, Mongolia},
  xtick=data, x tick label style={font=\small},
  y tick label style={font=\scriptsize},
  grid=major, grid style={very thin, gray!25},
  legend style={font=\scriptsize, at={(0.5,1.14)}, anchor=north, legend columns=4,
                draw=none, /tikz/every even column/.append style={column sep=4pt}},
  tick align=outside, tick pos=left,
]
\addplot[fill=gray!35, draw=gray!60!black] coordinates {
  (Singapore,\lnum{reach_sg_ascii}) (Australia,\lnum{reach_au_ascii})
  (Malaysia,\lnum{reach_my_ascii}) (India,\lnum{reach_in_ascii})
  (China,\lnum{reach_cn_ascii}) (Mongolia,\lnum{reach_mn_ascii})};
\addplot[fill=blue!30, draw=blue!60!black] coordinates {
  (Singapore,\lnum{reach_sg_script}) (Australia,\lnum{reach_au_script})
  (Malaysia,\lnum{reach_my_script}) (India,\lnum{reach_in_script})
  (China,\lnum{reach_cn_script}) (Mongolia,\lnum{reach_mn_script})};
\addplot[fill=orange!35, draw=orange!70!black] coordinates {
  (Singapore,\lnum{reach_sg_ascii}) (Australia,\lnum{reach_au_ascii})
  (Malaysia,\lnum{reach_my_ascii}) (India,\lnum{reach_in_ascii})
  (China,\lnum{reach_cn_ascii_nat}) (Mongolia,\lnum{reach_mn_ascii})};
\addplot[fill=green!45!black, draw=black] coordinates {
  (Singapore,\lnum{reach_sg_script}) (Australia,\lnum{reach_au_script})
  (Malaysia,\lnum{reach_my_script}) (India,\lnum{reach_in_script})
  (China,\lnum{reach_cn_full}) (Mongolia,\lnum{reach_mn_full})};
\legend{mono/EN, script/EN, mono/{+}native, script/{+}native}
\end{axis}
\end{tikzpicture}
\caption{Reachability by economy under all four conditions. The four Latin-script
economies are flat across every bar --- the treatments are no-ops where there is
nothing to translate and nothing the monolingual rule cannot read. China and
Mongolia move only on the fourth bar, where both treatments are applied together.
Every value plotted here is a \texttt{\textbackslash lnum} macro resolved from the
run records at compile time.}
\label{fig:reach}
\end{figure}

% ledger:strict
\claim{C1}
% ledger:endstrict

The structure of Table~\ref{tab:reach} and Figure~\ref{fig:reach} is the finding. Script-aware tokenisation
is necessary and not sufficient: China stays at \lnum{reach_cn_script} once its
text is tokenisable, because an English query and a Chinese statute share no
tokens whatever the tokeniser does. Native vocabulary is likewise necessary and
not sufficient: with the monolingual tokeniser, adding Chinese query phrases
leaves China at \lnum{reach_cn_ascii_nat}, because those phrases tokenise to
nothing on the way in. Only the two together recover anything.

\begin{figure}[t]
\centering
\begin{tikzpicture}[
  >=Stealth, font=\footnotesize,
  box/.style={draw, rounded corners=2pt, minimum height=6mm, inner xsep=4pt,
              font=\scriptsize, align=center},
  bad/.style={box, fill=red!10, draw=red!50!black},
  ok/.style={box, fill=blue!8},
  % `out` is a built-in TikZ key (to[out=45]); this style must not shadow it.
  msgbox/.style={box, fill=black!6, font=\scriptsize\itshape},
]
\node[ok] (q) {English indicator\\vocabulary};
\node[ok, below=7mm of q] (d) {Han-script\\provision};
\node[box, right=9mm of q, yshift=-8mm] (tok) {tokeniser\\\texttt{[a-z0-9]+}};
\node[ok, right=9mm of tok, yshift=8mm] (qt) {tokens\\\{shall, not, \ldots\}};
\node[bad, right=9mm of tok, yshift=-8mm] (dt) {tokens\\$\emptyset$};
\node[bad, right=9mm of qt, yshift=-8mm] (score) {BM25 $=0.0$\\every provision};
\node[msgbox, right=8mm of score] (msg) {``No provision\\found''};

\node[ok, below=17mm of d] (absent) {economy with\\no such law};
\node[msgbox] at (msg |- absent) (msg2) {``No provision\\found''};

\draw[->] (q.east) -- ++(3mm,0) |- (tok.west);
\draw[->] (d.east) -- ++(3mm,0) |- (tok.west);
\draw[->] (tok.east) -- ++(3mm,0) |- (qt.west);
\draw[->] (tok.east) -- ++(3mm,0) |- (dt.west);
\draw[->] (qt.east) -- ++(3mm,0) |- (score.west);
\draw[->] (dt.east) -- ++(3mm,0) |- (score.west);
\draw[->] (score) -- (msg);
\draw[->] (absent) -- (msg2);
\begin{scope}[on background layer]
\node[draw=red!45!black, dashed, rounded corners=3pt, inner sep=5pt,
      fit=(msg) (msg2)] (both) {};
\end{scope}
\node[right=1mm of both, font=\scriptsize\itshape, text=red!55!black, align=left]
  {byte-identical\\output};
\end{tikzpicture}
\caption{Why the failure survives review. A tokenisation mismatch and a genuine
regulatory absence produce the same output, and an absence is a legitimate and
common answer in a regulatory index. Nothing raises, nothing logs, and the exit
code is zero on both paths.}
\label{fig:silent}
\end{figure}

The operational point is not the magnitude but the mode. Lexical scoring
contributes the majority of the hybrid retrieval score in the deployed system, so
an economy in this state contributes a uniform zero to ranking. No exception is
raised, no log line is emitted, and the pipeline terminates normally, reporting
that no provision was found. That output is indistinguishable from the output for
an economy that genuinely has no such law --- which is a legitimate and common
answer in a regulatory index, and is precisely why the failure survives review.

\subsection{Widening the tokeniser is not free}

% ledger:strict
\claim{C2}
% ledger:endstrict

Under the monolingual rule the Mongolian distractors tokenise to nothing: they
occupy the index but can never score, and are effectively absent. Making the
tokeniser script-aware admits them, which changes the document-frequency and
length-normalisation statistics that BM25 shares across the whole collection \citep{robertson2009bm25}.
Australia's recall@10 falls from \lnum{r10_au_ascii} to \lnum{r10_au_script}
although not one byte of Australian tokenisation changed; Malaysia rises from
\lnum{r10_my_ascii} to \lnum{r10_my_script} and India from \lnum{r10_in_ascii}
to \lnum{r10_in_script}.

\begin{figure}[t]
\centering
\begin{tikzpicture}
\begin{axis}[
  ybar, width=0.86\linewidth, height=4.8cm,
  bar width=11pt, enlarge x limits=0.22,
  ymin=0, ymax=0.45, ylabel={recall@10}, ylabel style={font=\small},
  symbolic x coords={Australia, Malaysia, India},
  xtick=data, x tick label style={font=\small},
  y tick label style={font=\scriptsize},
  grid=major, grid style={very thin, gray!25},
  nodes near coords, nodes near coords style={font=\tiny, /pgf/number format/fixed,
    /pgf/number format/precision=4},
  legend style={font=\scriptsize, at={(0.5,1.16)}, anchor=north, legend columns=2,
                draw=none},
  tick align=outside, tick pos=left,
]
\addplot[fill=gray!35, draw=gray!60!black] coordinates {
  (Australia,\lnum{r10_au_ascii}) (Malaysia,\lnum{r10_my_ascii})
  (India,\lnum{r10_in_ascii})};
\addplot[fill=blue!35, draw=blue!60!black] coordinates {
  (Australia,\lnum{r10_au_script}) (Malaysia,\lnum{r10_my_script})
  (India,\lnum{r10_in_script})};
\legend{monolingual, script-aware}
\end{axis}
\end{tikzpicture}
\caption{The cost of widening the tokeniser, on three economies whose own
tokenisation did not change by a single byte. Admitting Cyrillic titles to the
index alters the document-frequency and length-normalisation statistics that
BM25 shares across the collection, and the ranking of Latin-script documents
moves with them --- down for Australia, up for Malaysia and India.}
\label{fig:cost}
\end{figure}

We report this because it is the kind of coupling that multi-region systems
acquire silently. Reachability, being a per-document property, is unchanged for
every Latin economy; only the rank-sensitive metrics move. A team measuring only
reachability would have concluded the change was free, and a team measuring only
recall on one economy would have concluded it was harmful.

\subsection{The headline metric is stable}

% ledger:strict
\claim{C3}
% ledger:endstrict

Reachability does not depend on which distractors were drawn, which is what one
should expect of a property of the query vocabulary and the tokeniser, and is
worth confirming rather than assuming. The rank-sensitive metrics do move with
the draw, which is why the seed is an axis of the grid rather than a fixed
constant, and why every cell in this paper carries its spread.

\subsection{The cheaper shortlist is not cheaper everywhere}

% ledger:strict
\claim{C4}
% ledger:endstrict

Mongolia loses nothing because its one reachable instrument already ranks first;
Malaysia loses two thirds of its recall because its instruments cluster just
inside the tenth position. The operational reading is that a shortlist budget
chosen on one jurisdiction's curve is the wrong budget for the others, and that
the choice is a per-economy one --- which is exactly the kind of parameter a
harness makes visible and an eyeball does not.

\section{Extending to other domains of law}
\label{sec:extending}

Nothing in the harness is specific to trade regulation. A project couples to it
through one manifest, and the manifest is the only coupling. Porting to a
different body of law --- competition, environmental, labour, tax --- requires
four things, and none of them are harness changes:

\begin{enumerate}
\item \textbf{A codified test per indicator.} The operative rule a provision must
enact, written so that a disagreement between two graders is a disagreement about
law rather than about wording. This is the expensive part, and it is legal work
rather than engineering work.
\item \textbf{A reference set.} Instrument-level answers from a source that is
authoritative in the domain. Ours is the framework's own expert panel; a
competition-law port might use a regulator's decisional practice.
\item \textbf{An entry point.} Any callable that takes a configuration and returns
metric names to numbers. The harness imports it; it never vendors the code.
\item \textbf{Native vocabulary per language.} The result above says this is not
optional in any multilingual jurisdiction, and that it cannot be substituted for
by a better tokeniser.
\end{enumerate}

Two design properties do the portability work. Identity is content-addressed, so
correctness of resume does not depend on remembering what was run. And the
experiment grid is declared rather than coded, so adding a jurisdiction, a script
or an ablation is a manifest edit.

We would flag one boundary honestly. The harness guarantees that a published
number traces to a recorded run; it does not guarantee that the run measured
anything worth knowing. Choosing indicators, building the reference set and
deciding what counts as a correct mapping remain domain expertise. What the
harness removes is the class of error where the number in the abstract stopped
matching the experiment behind it.

\section{Related work}

\paragraph{Legal NLP benchmarks.} LexGLUE assembles seven English legal
understanding tasks \citep{chalkidis2022lexglue}; LegalBench extends the idea to
162 tasks of legal \emph{reasoning}, built with practising lawyers
\citep{guha2023legalbench}; COLIEE has run statute and case-law retrieval and
entailment tasks for over a decade \citep{goebel2023coliee}. All three evaluate a
model against a corpus that is given. Our object is the layer beneath: whether
the pipeline that assembles the corpus can see the law at all. A benchmark whose
corpus is handed to it cannot expose a discovery or tokenisation failure, because
it has already been paid for.

\paragraph{Reliability of legal retrieval systems.} \citet{magesh2024hallucination}
is the closest work in spirit --- an audit of deployed systems rather than of
models, finding 17--33\% hallucination in tools sold as hallucination-free. The
difference is the failure mode. Their subject is a system that answers when it
should not; ours is a system that \emph{declines} to answer when it should have,
and does so in a form indistinguishable from a correct decline. A hallucination
is visible to anyone who checks the citation. An unreachable jurisdiction is
visible to nobody, because there is no citation to check.

\paragraph{Multilingual retrieval.} Mr.\ TyDi \citep{zhang2021mrtydi} and MIRACL
\citep{zhang2022miracl} established monolingual retrieval evaluation across
typologically diverse languages, and MIRACL in particular covers eighteen. Both
evaluate \emph{per language}: queries and corpus share a language, and each
language is scored on its own index. That design is right for measuring a
retriever and wrong for our question, which only arises when several scripts
share one index and one tokenisation rule, and where --- as
Section~\ref{sec:results} shows --- admitting one script changes the collection
statistics that rank the others.

Closest to our mechanism is \citet{ogundepo2022whitespace}, who show that
character $n$-gram tokenisation is a strong general substitute for
language-specific tokenizers in retrieval. We take that result as given and ask a
different question: not which tokeniser scores best, but what a deployed system
reports when it has the wrong one. The answer is not a lower score. It is a
uniform zero that the pipeline reports as an absence of law.

\paragraph{Attribution and grounding.} Retrieval-augmented generation with
citation grounding checks that a generated statement is supported by retrieved
text, and the pipeline studied here applies exactly that control: a mapping is
rejected unless its quoted snippet is verifiably present in the source document.
Such controls assume retrieval succeeded. Where an economy is unreachable, a
grounding check has nothing to object to --- there is no unsupported claim, only
an absence, and an absence is a valid and common answer in a regulatory index.
Grounding is a guard on the answer; nothing in it guards the question.

\paragraph{Provenance and reproducibility.} Experiment trackers, workflow engines
and artifact stores record what was run, and provenance over a pipeline's
artifacts is well understood as the missing discipline
\citep{samuel2020provenance}. What none of that stack does is check what was
\emph{written}: nothing objects when a number is typed into a manuscript and the
experiment is then re-run. Our contribution to that literature is narrow and,
we think, load-bearing --- extending provenance across the last hop, from the
record to the sentence that cites it, and making the build fail rather than the
reader guess.
\section{Limitations}

\textbf{This measures the lexical floor, not end-to-end accuracy.} The unit here
is the instrument title as its portal serves it, not full provision text, and the
metric is lexical reachability, not mapping F1. Provision-level extraction for
China, India and Mongolia requires a built corpus that does not yet exist, and a
live crawl is neither reproducible nor free. We report the layer we can measure
reproducibly and decline to report the one we cannot; the harness makes that
distinction visible rather than letting an unmeasured claim pass as a measured
one.

\textbf{Reachability is a floor, not a ceiling.} A reachable instrument may still
rank poorly, and a dense retrieval stage may recover documents that lexical
scoring misses. The deployed system weights lexical scoring at the majority of
its hybrid score, so a uniform zero there is materially damaging, but it is not
the whole retrieval system.

\textbf{Reference sets are not exhaustive.} The panel recorded the instruments it
accepted, not every instrument that would satisfy each test. A system may surface
a legitimate instrument the panel did not record, which this evaluation scores as
a miss. This biases all reported figures downward and biases them equally across
conditions, so the comparisons hold even though the absolute levels understate.

\textbf{The Mongolian native vocabulary is unvalidated.} It is a seed list of
statutory stems, and Mongolian is agglutinative, so a lexical index may hold an
inflected form that the stem never matches. The Mongolian recovery reported here
should be read as a lower bound from an unaudited vocabulary.

\section{Conclusion}

A legal-evidence pipeline that cannot be audited is a pipeline whose failures are
invisible, and the multilingual failures are the most invisible of all, because
they present as the same output a correct system produces on a jurisdiction with
no such law. We built a harness that refuses to publish a number it cannot trace
to a run, pointed it at our own retrieval layer, and found an economy that had
been silently absent under a tokenisation rule that was correct for every
jurisdiction it was developed against.

The finding is small and the discipline is not. Every number in this paper,
including the ones in the abstract, was substituted from a record on disk at
compile time, and the build fails if any of them stops resolving. That property
is what we are proposing, more than the measurement it happens to carry.

\label{ledger:endmain}

\bibliographystyle{iclr2027_conference}
\bibliography{references}

\appendix

\section{Case studies}
\label{app:cases}

Three instruments, one per script, walked through the same code the benchmark
measures. The aggregate result says an economy is unreachable; these show why,
token by token. The Singaporean case is the control --- Latin script throughout,
so no condition changes anything.

The native titles are reproduced as images. This is not a stylistic choice: the
TeX installation used for this build has no CJK package and no Cyrillic
encoding, and transliteration is not available to us, because the pipeline's own
rule is that statutory text is carried and never rewritten. What appears below is
the statute's own text rather than a romanisation of it. Non-Latin tokens are
counted rather than printed for the same reason, and counted separately from
Latin ones so that a mixed-script query shows which half is doing the work.

\input{cases/cases.tex}

\section{Generated charts}
\label{app:charts}

Emitted by the \texttt{figures} stage as pgfplots, from the same metrics file as
every table below. Bars are cell means and whiskers run min to max across the
seed axes; a cell measured at one seed draws no whisker, because a whisker of
zero would read as a measured spread of zero. Condition labels are the full cell
configuration.

\subsection{Reachability}
\input{generated/figures/t_reach_reachable.tex}

\subsection{recall@5}
\input{generated/figures/t_reach_recall_at_5.tex}

\subsection{recall@10}
\input{generated/figures/t_reach_recall_at_10.tex}

\subsection{Mean reciprocal rank}
\input{generated/figures/t_reach_mrr.tex}

\section{Full cell listing}

The tables below are generated from the metrics file and are not hand-edited; the
harness verifies at build time that each matches what the current records
produce.

\subsection{Reachability}
{\scriptsize\input{generated/tables/t_reach_reachable.tex}}

\subsection{recall@5}
{\scriptsize\input{generated/tables/t_reach_recall_at_5.tex}}

\subsection{recall@10}
{\scriptsize\input{generated/tables/t_reach_recall_at_10.tex}}

\subsection{Mean reciprocal rank}
{\scriptsize\input{generated/tables/t_reach_mrr.tex}}

\end{document}
