%% Generated diagram fragment. The paper's preamble supplies tikz, amsmath and
%% the libraries; this file is the picture only. Laid out landscape: the scoring
%% stages run left to right so the figure costs width rather than page height.
\begin{tikzpicture}[
  >=Stealth,
  box/.style={draw, rounded corners=2pt, minimum height=7.5mm, align=center,
              font=\scriptsize, inner xsep=3pt},
  scorer/.style={box, fill=blue!7, minimum width=27mm},
  src/.style={box, fill=black!5, minimum width=32mm},
  fuse/.style={box, fill=green!8, minimum width=30mm},
  llm/.style={box, fill=orange!10, minimum width=30mm},
  gate/.style={box, fill=red!8, minimum width=27mm},
  note/.style={font=\tiny\itshape, text=black!55, align=center, inner sep=1pt},
  flow/.style={->, semithick, black!65},
]

% ---- inputs, then the three scorers in parallel ------------------------
\node[src] (corpus) {provisions of one economy};
\node[src, below=3mm of corpus] (query)
  {indicator query: title $+$ EN $+$ native terms};

\node[scorer, right=14mm of corpus, yshift=4mm]  (bm25)  {BM25};
\node[scorer, below=2.5mm of bm25]               (dense) {dense bi-encoder};
\node[scorer, below=2.5mm of dense]              (ce)    {cross-encoder};

\foreach \t in {bm25, dense, ce} {
  \draw[flow] (corpus.east) -- (\t.west);
  \draw[flow] (query.east)  -- (\t.west);
}
\node[note, right=1.5mm of bm25,  text width=26mm, align=left] {script-aware: bigrams for no-space scripts};
\node[note, right=1.5mm of dense, text width=26mm, align=left] {cross-lingual by construction};
\node[note, right=1.5mm of ce,    text width=26mm, align=left] {English only; \emph{disabled}, never swapped};

% ---- the chain, left to right ------------------------------------------
\node[fuse, below=15mm of query.south east, anchor=west, xshift=-14mm] (fuse)
  {weighted fusion};
\node[fuse, right=7mm of fuse] (short) {shortlist $k$};
\node[llm,  right=7mm of short] (llm)  {LLM grader};
\node[gate, right=7mm of llm]   (conf) {confidence};
\node[box, fill=black!4, right=7mm of conf, minimum width=42mm] (route)
  {quarantine $\mid$ review $\mid$ accept};

\draw[flow] (fuse) -- (short);
\draw[flow] (short) -- (llm);
\draw[flow] (llm) -- (conf);
\draw[flow] (conf) -- (route);
\draw[flow] (dense.south) ++(0,-2mm) |- ([yshift=4mm]fuse.north) -- (fuse.north);

\node[note, below=1.5mm of fuse,  text width=32mm]
  {$\alpha=0.65$ lexical\,:\,dense,\\then $\tfrac12$ cross-encoder};
\node[note, below=1.5mm of short, text width=32mm]
  {$\min(n,450,\max(40,\lceil 0.05n\rceil))$;\\grade all when $n\le 80$};
\node[note, below=1.5mm of llm,   text width=32mm]
  {target shown with \emph{all} siblings;\\grades, never authors a citation};
\node[note, below=1.5mm of conf,  text width=27mm]
  {four weighted signals; one is a self-check};
\end{tikzpicture}
