%% Generated diagram fragment. The paper's preamble supplies tikz, amsmath
%% and the libraries; this file is the picture only.
\begin{tikzpicture}[
  >=Stealth,
  box/.style={draw, rounded corners=2pt, minimum height=8mm, align=center,
              font=\scriptsize, inner xsep=3pt},
  scorer/.style={box, fill=blue!7, minimum width=33mm},
  src/.style={box, fill=black!5, minimum width=40mm},
  fuse/.style={box, fill=green!8, minimum width=44mm},
  llm/.style={box, fill=orange!10, minimum width=62mm},
  gate/.style={box, fill=red!8, minimum width=44mm},
  note/.style={font=\tiny\itshape, text=black!55, align=center, inner sep=1pt},
  side/.style={font=\tiny\itshape, text=black!55, align=left, inner sep=1pt},
  flow/.style={->, semithick, black!65},
]

% ===== inputs ===========================================================
\node[src] (corpus) {provisions of one economy};
\node[src, right=12mm of corpus] (query)
  {indicator query\\[-1pt] \scriptsize title $+$ English terms $+$ native terms};

% ===== three scorers, in parallel =======================================
\node[scorer, below=13mm of corpus, xshift=-20mm] (bm25)  {BM25};
\node[scorer, right=5mm of bm25]                  (dense) {dense bi-encoder};
\node[scorer, right=5mm of dense]                 (ce)    {cross-encoder};

\node[note, below=1.5mm of bm25,  text width=33mm] {script-aware tokeniser:\\bigrams for no-space scripts};
\node[note, below=1.5mm of dense, text width=33mm] {multilingual MiniLM,\\cross-lingual by construction};
\node[note, below=1.5mm of ce,    text width=33mm] {English only; \emph{disabled}, never\\swapped, for other languages};

\foreach \t in {bm25, dense, ce} {
  \draw[flow] (corpus.south) -- (\t.north);
  \draw[flow] (query.south)  -- (\t.north);
}

% ===== fusion and shortlist =============================================
\node[fuse, below=18mm of dense, minimum width=66mm] (fuse)
  {$s_i=\tfrac12\!\left[\max\!\big(0,\;
     \alpha\,\tfrac{b_i}{\max_j b_j}+(1{-}\alpha)\tfrac{\cos_i+1}{2}
     +\beta_i-\pi_i\big)\right]+\tfrac12\,\sigma(c_i)$\\[1pt]
   \scriptsize $\alpha=0.65$; \; $\beta\le 0.30$ phrase bonus; \;
   $\pi\le 0.20$ sibling penalty};
\foreach \t in {bm25, dense, ce} \draw[flow] (\t.south) ++(0,-7.5mm) -- (fuse.north);

\node[fuse, below=6mm of fuse, minimum width=66mm] (short)
  {$k=\min\!\big(n,\,450,\,\max(40,\lceil 0.05n\rceil)\big)$ \;---\; or grade all $n$ when $n\le 80$};
\draw[flow] (fuse) -- (short);

% ===== grading ==========================================================
\node[llm, below=7mm of short] (llm)
  {LLM grader --- target indicator shown \emph{together with all siblings}};
\draw[flow] (short) -- (llm);
\node[side, right=3mm of llm, text width=30mm]
  {forces an explicit choice between adjacent indicators; grades evidence, never
   authors a citation. A returned subsection is kept only if it occurs
   literally in the snippet};

% ===== confidence and routing ===========================================
\node[gate, below=8mm of llm] (conf) {confidence: four weighted signals};
\draw[flow] (llm) -- node[note, right, xshift=1.5mm, text width=34mm, align=left]
  {structured JSON: operative rule,\\satisfies target, better sibling, subsection} (conf);
\node[side, left=3mm of conf, text width=30mm, align=right]
  {one signal is a self-check: is the quoted snippet literally present in the
   source document?};

\node[box, fill=black!4, below=6mm of conf, minimum width=62mm] (route)
  {quarantine \quad$\mid$\quad human review \quad$\mid$\quad auto-accept};
\draw[flow] (conf) -- (route);
\end{tikzpicture}
