# Prompt sửa lần 2 — huỷ 3 slide kết mới, quay về duplicate + highlight

```
Undo change 4 — I was wrong to ask for new slides.

Discard slides 12, 13 and 14 you just built. No new slides, no new charts, no new copy anywhere in
this deck. Every slide I present is a duplicate of a slide that already exists in the submitted
deck, with one region highlighted and the rest dimmed — the same rule as slides 1 through 11.

My closing minute is three quick mentions, and all three already have their words on existing
slides. Rebuild 12-14 as duplicates:

12. Duplicate "Evaluation & Performance" · highlight the "Cost per economy" tile, the "Response
    time" tile, and the transparency note at the bottom of the slide · rail: whole pipeline
    neutral, no stage lit · note under the rail: measured, recorded in every output file

    (This is the second time this slide appears — slide 2 highlights the Citation fidelity,
    Coverage, OCR and Discovery precision tiles. Make sure the two duplicates highlight clearly
    different regions so it reads as a deliberate second pass, not a repeat.)

13. Duplicate "References" · highlight the "AI MODELS" block and the "OPEN-SOURCE LIBRARIES" block ·
    rail: whole pipeline neutral, no stage lit · note under the rail: fully open stack, swappable
    by config

14. Duplicate "Scalability & Impact" · highlight the "SCALING OUT" column and the "ROUND 2 ROADMAP"
    band · rail: `Discover` and `Extract` in the "slot" state, captioned "+1 entry strategy per
    economy" and "+1 extraction profile per drafting style" · note under the rail: every item plugs
    into a slot the system already has

Everything from changes 1, 2 and 3 stands: slides 1-4 of the original deck untouched, no
"Technology & Innovation" slide in my sequence, and the rail's visual design is entirely yours.
```
