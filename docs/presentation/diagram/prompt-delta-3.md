# Prompt sửa lần 3 — đảo thứ tự mở đầu, bớt highlight

```
Three small changes.

1. SWAP MY FIRST TWO SLIDES. The "Evaluation & Performance" duplicate now comes first, and
   "System Architecture" second. I open on the one requirement that outranks everything —
   the quoted clause must be exactly what the law says and provable against the official
   source — and only then show the pipeline built around it.

2. FEWER HIGHLIGHTS PER SLIDE. Wherever a slide currently lifts several blocks, keep only
   the one that carries the point:
   - Evaluation & Performance (now slide 1): highlight ONLY the "Citation fidelity 100%"
     tile. The OCR, Coverage and Discovery precision tiles stay dimmed with the rest — I
     mention them in passing, they are not the point.
   - Technology / Backend slides where I named a card plus a numbered point in the right
     column: drop the numbered point, keep only the card.
   - Map & Verify: keep "Sibling-aware LLM mapping" and the Singapore P6-I4 example box;
     drop "Cross-model second opinion" from the highlight.
   One focus per slide. Everything else stays present but dimmed, as before.

3. On the "Scalability & Impact" slide, make sure "Real-time monitoring" is inside the
   highlighted region — the scheduled re-crawl that diffs against cached results is the
   part I speak to.
```
