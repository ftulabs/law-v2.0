# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# env / secrets — NEVER commit real keys
.env
.env.local
.streamlit/secrets.toml

# runtime outputs (keep the dir, not the artefacts)
outputs/*
!outputs/.gitkeep
logs/

# Zone 1 downloaded law bodies — runtime, content-hashed, re-fetchable
data/cache/

# OS / editor
.DS_Store
.vscode/
.idea/

# Claude Code personal settings (permission allowlist etc.)
.claude/settings.local.json

# local run logs
streamlit*.log

# generated: white paper copied from docs/whitepaper.html for Streamlit static serving
frontend/static/

# submission build artifacts (zips generated for the JotForm upload)
FTU-VeriTrade_*.zip

# separate AI4Econ paper project (kept out of the hackathon repo)
docs/paper/

# personal working docs — internal notes, not deliverables
docs/precompute-corpus.md
docs/video-script.md

# organiser-provided reference PDFs (not our authored work)
docs/presentation/Pitching Rules.pdf

# UI/UX Pro Max skill — vendored tooling, reinstall with:
#   npx ui-ux-pro-max-cli@latest init --ai claude
.claude/skills/

# internal planning deck (not a submission deliverable)
docs/roadmap-final-round.html
